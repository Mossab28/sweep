{
  "name": "@mossab/sweep",
  "version": "0.1.0",
  "description": "AI-powered file cleanup & organizer CLI",
  "license": "MIT",
  "type": "module",
  "bin": { "sweep": "dist/cli.js" },
  "engines": { "node": ">=20" },
  "files": ["dist"],
  "scripts": {
    "test": "vitest run",
    "build": "tsup src/cli.ts --format esm --target node20 --clean",
    "dev": "tsx src/cli.ts",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.0",
    "chalk": "^5.3.0",
    "commander": "^12.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0",
    "@types/node": "^20.11.0"
  }
}
